import { styled } from "@mui/material/styles";
const Root = styled("div")(({ theme }) => ({
  //   backgroundColor: "red",
  marginTop: "100px",
  "& .Registration-form": {
    // backgroundColor: "pink",
    width: "50%",
    margin: "0 auto",
  },
}));

export default Root;
